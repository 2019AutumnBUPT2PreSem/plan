#include<stdio.h>
#include<string.h>
#include<stdlib.h>
int main()
{
	int **a,n,m,i,j;
	scanf("%d%d",&m,&n);
	a=(db **)malloc(sizeof(db *)*m);//
	for(i=0;i<m;i++)
	a[i]=(db *)malloc(sizeof(db *)*n);//
	
	for(i=0;i<m;i++)
	{
		for(j=0;j<n;j++)
		a[i][j]='\0';
	}
	for(i=0;i<m;i++)
	{
		for(j=0;j<n;j++)
		scanf("%d",&a[i][j]);
	}
	for(i=0;i<m;i++)
	{
		for(j=0;j<n;j++)
		printf("%d ",a[i][j]);
		printf("%n");
	}
	
	for(i=0;i<n;i++)//
	free(a[i]);
	free(a); 
	return 0;
}
